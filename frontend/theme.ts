import { createTheme } from '@mantine/core';

export const theme = createTheme({
  colors: {
    qclRed: ['#f5e7e7', '#ecced0', '#e2b6b8', '#d89da0', '#cf8589', '#c56c71', '#bb5459', '#b13b41', '#a8232a', '#9e0a12'],
  },
  primaryColor: 'qclRed',
});
